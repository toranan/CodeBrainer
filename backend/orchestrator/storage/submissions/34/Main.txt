#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int n;
    cin >> n;
    
    int maxCoin = 0;
    for (int i = 0; i < n; i++) {
        int coin;
        cin >> coin;
        maxCoin = max(maxCoin, coin);
    }
    
    cout << maxCoin << endl;
    return 0;
}