spring:
  application:
    name: codebrainer-orchestrator
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true&prepareThreshold=0}
    username: ${DATABASE_USERNAME:postgres.sqwobsmtrgjuhgymfwtl}
    password: ${DATABASE_PASSWORD:qpwoe1234}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 2
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 300000
      max-lifetime: 900000
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
  flyway:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info

storage:
  type: ${STORAGE_TYPE:local}  # local or supabase
  base-path: ./storage
  supabase:
    url: ${SUPABASE_URL:}
    key: ${SUPABASE_SERVICE_ROLE_KEY:}
    bucket: ${SUPABASE_BUCKET:codebrainer-problems}

judge0:
  api-url: http://54.252.182.149:2358
  callback-url: http://localhost:8080/internal/judge0/callback
  callback-enabled: false

queue:
  submission:
    name: submissions.queue
    routing-key: submissions.key
    exchange: submissions.exchange

codebrainer:
  submission:
    poll-delay-ms: 1000
    max-poll-attempts: 30

logging:
  level:
    org.springframework.web.client: DEBUG
    org.apache.http: DEBUG

gemini:
  api-key: ${GEMINI_API_KEY:}
  model: ${GEMINI_MODEL:gemini-1.5-pro}
  enabled: ${GEMINI_ENABLED:false}

